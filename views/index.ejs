<%- include("partials/header") %>

<h2>Latest Products</h2>

<div>
  <% products.forEach(p => { %>
    <div>
      <h3><%= p.name %></h3>
      <p>₹<%= p.price %></p>
      <a href="/products/<%= p._id %>">View</a>
    </div>
  <% }) %>
</div>

<%- include("partials/footer") %>
